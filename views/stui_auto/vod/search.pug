//- Source: search.html
//- ⚠ Manual work: 4 item(s)
//-
//-   MANUAL: {maccms:vod num="10" paging="yes" pageurl="vod/search" order="desc" by="time"} needs controller pre-query
//-   MANUAL: {maccms:type num="10" order="asc" by="sort" ids="1,2"} needs controller pre-query
//-   MANUAL: Complex URL helper: {:mac_url_vod_play($vo,['sid'=>1,'nid'=>1])}
//-   MANUAL: 1 inline conditionals embedded in text — needs structural Pug refactor

html
  head
    include ../seo/vod_search
    include ../block/include
  body
    include ../block/head
    div.container
      div.row
        div.col-lg-wide-75.col-xs-1.padding-0
          div.stui-pannel.stui-pannel-bg.clearfix
            div.stui-pannel-box
              div.stui-pannel_hd
                div.stui-pannel__head.active.bottom-line.clearfix
                  h3.title
                    img(src="#{maccms.pathTpl}statics/icon/icon_27.png")
                    | #{param.wd}
              div.stui-pannel_bd
                ul.stui-vodlist__media.col-pd.clearfix
                  //- MANUAL: maccms block (needs controller pre-query)top-line-dot {{INLINE_ENDIF}}clearfix">
                  div.thumb
                    a.v-thumb.stui-vodlist__thumb.lazyload(href="`/vod/detail/id/${item._id}.html`", title="#{item.name}", data-original="#{item.pic}")
                      span.play.hidden-xs
                      span.pic-text.text-right  // MANUAL: Inline conditional - needs structural Pug conversion
                  div.detail
                    h3.title a(href="`/vod/detail/id/${item._id}.html`") #{item.name}
                    p span.text-muted 导演： #{item.director || '内详'}
                    p span.text-muted 主演： #{item.actor.substring(0, 18) || '内详'}
                    p.hidden-mi span.text-muted 类型： #{item.class} span.split-line span.text-muted 地区： #{item.area || '内详'} span.hidden-xs span.split-line span.text-muted 年份： #{item.year || '内详'}
                    p.margin-0
                      a.btn.btn-min.btn-primary(href="(TODO: mac_url complex call)") 立即播放 &nbsp;&nbsp; a.btn.btn-min.btn-default(href="`/vod/detail/id/${item._id}.html`") 查看详情
        include ../block/page
      div.col-lg-wide-25.stui-pannel-side.hidden-md.hidden-sm.hidden-xs
        //- MANUAL: maccms block (needs controller pre-query)
  include ../block/foot
